## What & why

<!-- One or two sentences in plain language: what does this change, and why? -->

Closes #

## How I tested it

<!-- e.g. "ran npm run dev and checked the header on mobile and desktop",
     "tested with VoiceOver", "checked the built output with npm run preview" -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] I looked at the change in a browser, not just the terminal
- [ ] No secrets in the diff (nothing new under `VITE_` that shouldn't be public)

<!-- If you touched public/_headers, the SEO map, or index.html's inline
     script, say so here — those three have gotchas documented in
     CONTRIBUTING.md. -->

> **Reminder:** merging to `main` deploys to production (marrahub.com.au).
