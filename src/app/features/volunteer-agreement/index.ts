// Public surface of the volunteer-agreement feature.
// Import from here only — the internal files may be reorganised when this folder
// is lifted into the Hub SaaS.
export { VolunteerAgreementPage } from './VolunteerAgreementPage';
export { volunteerAgreementConfig } from './config';
export type {
  VolunteerAgreementFields,
  VolunteerAgreementSubmission,
} from './types';
