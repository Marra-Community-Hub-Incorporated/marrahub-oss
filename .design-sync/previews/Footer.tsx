import React from 'react';
import { Footer } from 'marrahub-website';

// Footer takes no props — it renders the full site footer (contact details,
// quick links, acknowledgement of country).
export const Default = () => <Footer />;
